-- ══════════════════════════════════════════════════════════════════════════
-- Migration: เวลานัด (scheduled_time) ไม่บังคับอีกต่อไป
-- เพราะ AI จะคำนวณลำดับงานจากระยะทาง+ความสำคัญให้อัตโนมัติ (ดู src/lib/maps.ts)
-- ถ้าลูกค้ากำหนดเวลามาก็ยังใส่ได้ตามปกติ และ AI จะเอาไปคิดรวมเป็นเงื่อนไข "ต้องถึงก่อนเวลานี้" ด้วย
-- รันไฟล์นี้ใน Supabase SQL Editor
-- ══════════════════════════════════════════════════════════════════════════

ALTER TABLE jobs ALTER COLUMN scheduled_time DROP NOT NULL;
